<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica123">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BateriaActivity"></activity>
        <activity android:name=".CalendarActivity" />
        <activity android:name=".ConversionActividad" />
        <activity android:name=".NumeroActividad" />
        <activity android:name=".CamaraActivity" />
        <activity android:name=".recyclerView" />
        <activity android:name=".registrarDatos" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecibirParametroActivity" />
        <activity android:name=".PasarParametro" />
        <activity android:name=".ActividadBuscar" />
        <activity android:name=".ActividadRegistrar" />
        <activity android:name=".ActividadLogin" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>